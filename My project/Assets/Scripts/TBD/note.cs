/// remove all played and reset timer tag on card's pages on discard
/// add a default effect for token, to remove by default remove itself from entity after clash
/// when play the card, remember to expose the rigid and stuff whenever draw, discard and play with seperate tag